// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: device_tokens.sql

package sqlcgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const listDeviceTokens = `-- name: ListDeviceTokens :many
SELECT id, user_id, platform, token, created_at, updated_at FROM device_tokens
WHERE user_id = $1
ORDER BY created_at ASC
`

func (q *Queries) ListDeviceTokens(ctx context.Context, userID pgtype.UUID) ([]DeviceToken, error) {
	rows, err := q.db.Query(ctx, listDeviceTokens, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []DeviceToken{}
	for rows.Next() {
		var i DeviceToken
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Platform,
			&i.Token,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const registerDevice = `-- name: RegisterDevice :one
INSERT INTO device_tokens (user_id, platform, token)
VALUES ($1, $2, $3)
ON CONFLICT (token) DO UPDATE SET
    user_id = EXCLUDED.user_id,
    platform = EXCLUDED.platform,
    updated_at = now()
RETURNING id, user_id, platform, token, created_at, updated_at
`

type RegisterDeviceParams struct {
	UserID   pgtype.UUID `json:"user_id"`
	Platform string      `json:"platform"`
	Token    string      `json:"token"`
}

func (q *Queries) RegisterDevice(ctx context.Context, arg RegisterDeviceParams) (DeviceToken, error) {
	row := q.db.QueryRow(ctx, registerDevice, arg.UserID, arg.Platform, arg.Token)
	var i DeviceToken
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Platform,
		&i.Token,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const removeDeviceToken = `-- name: RemoveDeviceToken :exec
DELETE FROM device_tokens
WHERE token = $1
`

func (q *Queries) RemoveDeviceToken(ctx context.Context, token string) error {
	_, err := q.db.Exec(ctx, removeDeviceToken, token)
	return err
}
